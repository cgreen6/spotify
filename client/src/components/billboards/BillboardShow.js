import { useState } from "react";
import BillboardForm from "./billboardForm";
import { link } from 'react-router-dom';

const BillboardShow = ({ id, genre, deleteBillboard, updateBillboard }) => {
  const [editing, setEdit] = useState(false)

  return (
    <>
      <h3>{title}</h3>
    </>
  )}